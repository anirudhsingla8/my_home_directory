# Backend API Base URL
# Default is http://localhost:3000 (or http://10.0.2.2:3000 for Android emulator) if not provided.
# Replace with your local machine's IP address if testing on a physical device over LAN.
EXPO_PUBLIC_API_BASE_URL="http://192.168.1.100:3000"
